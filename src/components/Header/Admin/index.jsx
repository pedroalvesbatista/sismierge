import React from 'react'
import TabsAdmin from '../../Tabs'
import { 
    Area ,
    TitleArea,
    SearchArea,
    IconSearch,
    Input,
    Div
} from './styles'

function HeaderAdmin({ co2, co2t }) {
    
  return (
    <Area>
        {/* <TitleArea> {title  ? title === 'Inicio' ? 'Visão geral' : title : 'Visão geral'} </TitleArea> */}
        <Div />
    </Area>
  )
}

export default HeaderAdmin